name: Kirill love pasta

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      train_path: path
      paramC: {type: float, default: 0.9}
    command: "python train.py {train_path} {paramC}"
